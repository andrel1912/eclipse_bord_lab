[project]
name = "eclipsebord"
version = "0.1.0"
description = "Solar eclipse dashboard"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "ipykernel",
    "matplotlib>=3.11.1",
    "pandas>=3.0.5",
    "uvicorn>=0.52.4",
]

[tool.uv.workspace]
members = [
    "backend",
    "frontend",
]
